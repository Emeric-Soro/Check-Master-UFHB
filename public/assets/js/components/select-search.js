/* Legacy select-search init placeholder */
(function (window) {
  'use strict';

  window.CM = window.CM || {};
  window.CM.selectSearch = window.CM.selectSearch || {
    init: function () {
      // La logique principale est inline dans le composant PHP select-search.
    }
  };
})(window);
